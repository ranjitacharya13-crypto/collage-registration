{
  "name": "aura-2026",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,green \"vite\" \"node --env-file-if-exists=.env --watch server.js\"",
    "api": "node --env-file-if-exists=.env server.js",
    "mcp": "node --env-file-if-exists=.env mcp-server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node --env-file-if-exists=.env server.js",
    "export": "node --env-file-if-exists=.env scripts/export.js",
    "test": "node --test test/**/*.test.js",
    "check-db": "node --env-file-if-exists=.env scripts/check-db.js"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "three": "^0.165.0",
    "troika-three-text": "^0.52.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.4.21"
  },
  "description": "Interactive symposium journey with a local-first registration API and an optional local AI MCP server.",
  "main": "server.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=22.5.0"
  }
}